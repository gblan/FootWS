<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:camel="http://camel.apache.org/schema/spring"
	xsi:schemaLocation="
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans.xsd
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context.xsd
http://camel.apache.org/schema/spring
http://camel.apache.org/schema/spring/camel-spring.xsd">
	<camel:camelContext id="camel">
		<camel:route>
			<camel:from uri="activemq:fournisseur.requestQueue" />

			<camel:choice>
				<camel:when>
					<!-- ASYNCHRONE MAIL RESULT -->
					<camel:simple>${headers.operationName} == 'sendMail'</camel:simple>
					
					<camel:bean ref="footCamel" method="getTeamRouteCamel" />					
					<camel:bean ref="log" method="logginCamelXml" />					
					<camel:bean ref="mail" method="sendMailCamel" />
					

				</camel:when>
				<camel:when>
					<!-- SYNCHRONE XML RESULT -->
					<camel:simple>${headers.operationName} == 'returnXml'</camel:simple>

					<camel:bean ref="footCamel" method="getTeamRouteCamel" />
					<camel:bean ref="log" method="logginCamelXml" />
					<camel:to uri="activemq:fournisseur.responseQueue" />
				</camel:when>
			</camel:choice>

		</camel:route>
	</camel:camelContext>
	<bean id="activemq" class="org.apache.camel.component.jms.JmsComponent">
		<property name="connectionFactory">
			<bean class="org.apache.activemq.ActiveMQConnectionFactory">
				<property name="brokerURL" value="tcp://localhost:61616" />
			</bean>
		</property>
	</bean>
	
	<bean id="footCamel" class="et5.service.camel.FootballCamel" />
	<bean id="mail" class="et5.service.camel.SendMailCamel" />
	<bean id="log" class="et5.service.camel.LogginCamel" />
</beans>